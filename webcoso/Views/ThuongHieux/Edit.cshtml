@model webcoso.Models.ThuongHieu

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}


<div id="layoutSidenav_content">
    <main>
        <div class="container-fluid px-4">
            <h2 style="text-align: center; margin-top: 20px; font-weight: bold; color: red;">Chỉnh Sửa Thương Hiệu</h2>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-horizontal" style="width:55%;margin:0 auto">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    @Html.HiddenFor(model => model.MaTH)

                    <div style="display:flex;justify-content:center;align-items:center;flex-direction:column;">
                        <div style="width:100%">
                            <strong class="control-label col-md-10">Tên Thương hiệu</strong>
                            <div style = "width:100%;padding:10px">
                                @Html.EditorFor(model => model.TenTH, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.TenTH, "", new { @class = "text-danger" })
                            </div>
                            <div class="col-md-10 text-center">
                                <input type="submit" value="Lưu" class="btn btn-info" /> |
                                <a class="btn btn-success" href='@Url.Action("Index", "ThuongHieux")' style="margin: 30px 0;">
                                    Trở về
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </main>
</div>